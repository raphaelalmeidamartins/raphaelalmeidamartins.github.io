import { createTheme } from '@mui/material';

const theme = createTheme({
  mode: 'dark',
  typography: {
    fontFamily: '"Kantumruy Pro", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
